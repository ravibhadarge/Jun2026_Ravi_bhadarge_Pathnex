---
- name: create config file for dev-server
  hosts: dev-web-server
  become: yes
  tasks: 
    - name: write content to file
      copy:
        dest: /etc/deve-server.conf
        content: |  
           Welcome to dev-server for dev
           develper user can write the code
  
resource "aws_db_instance" "devRDS"
  allocatated_storage         = 200
  engine                      = "mysql"
  instance_class              = "db.r7g.xlarge"
  name                        = "devdb"
  username                    = "admin"
  password                    = "pawword123"
  skip_final_snapshot         = true
}

apiVerion: apps/v1
kind: Deployment
metatada:
  name: dev-deployment
spec:
  replicas: 3
  statergy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      MaxUnavailable: 1
  selector:
    matchLabels:
      app: roll
  template:
    metadata:
      labess:
        app: roll
    spec: 
      containers:
        - name: web
          image: nginx



#!/bin/bash
SOURCE="/var/log"
DEST="/backup/ravi-$(date +%F).tar.gz"
tar -czf %DEST $SOURCE
echo "backup created at: $DEST" 


pipeline {
     agent de-server
     environment {
         STAGE_NAME = "dev"
     }
     stages {
         staeg('checkout')
           steps {
               checkout scm
             }
     }
     stage('build')
         stage('build')
            steps {  
               sh 'mvn clean complie'
            }
       }
   }
}




stages: 
  - build

build-main:
   stage:build
   image: meven:3.8.1-jdk-21
   script:
     - mvn clean complie
   only  
    - main

build-dev:
  stage: build
  image: maven:3.8.1-jdk-21 
  script:
    - mvn clean complie
  only:
   - dev

================================================================
L2

---
- name: setup auto scaling group
  hosts: dev-server1  
  become: yes
  tasks: 
    - name: create launch configuration
      ec2_asg:
        name: ravi-asg
        region: us-east-1
        launch_config_name: ravi-launch-config
        min_size: 1
        max_size: 3
        desired_capacity: 2
        vpc_zone_identifier: subnet-12345678

resource "aws_lb" "ravi_lb" {
  name               = "ravi-1b"
  internal           = false
  load_balancer_type = "application"
  security_groups    = [aws_security_group.all_ssh_http.id]
  subnets            = [aws_subnet.public.id]
}
resource "aws_instance" "ravi-ec2"
  ami              = "ami-12356789gyee"
  instance_type    = "m9g.2xlarge"
  security_group   = [aws_security_group.allow_sh_http.name]
    tags = {
     Name = "ravi-ec2"
  }
}



   
apiVersion: apps/v1
kind: Deployment
metadata: 
  name: prod-deployment
spec: 
  replicas: 5
  selector:
    matchLabel:
      app: ravi-web-app 
  template:
    metadata:
      labels:
        app: ravi-web-app
    spec:
      containers:
          - name: web
            image: nginx
            ports:
              - containerPort: 8081
---
apiVersion: autoscalling/v2
kind: HorizontalAutoscaler
metadta:
  name: ravi-web-app
spec:
  scaleTargetRef:
    apiiVersion: apps/v1
    kind: Deployment
    name: ravi-web-app
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80



pipeline {  
    agent dev-server
    stages { 
        stage('build & test')
           parallel {
              stage('build')
                 steps {
                    echo 'building project...'
                 }
              }
              stage('test') {
                    steps {
                        echo 'Running tests...'
                 }
              } 
          }
      }
      stage('deploy') {
            steps {
                echo 'deploying project...'
            }
        }
    }
}


stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - docker build -t pathnex-web-app .

test:
  stage: test
  script:
    - docker run pathnex-web-app npm test

deploy:
  stage: deploy
  script:
    - kubectl apply -f kubernetes/deployment.yaml



FROM ubuntu
VOLUME ["/opt/pathnex/data"]
CMD ["sleep", "1000"]


docker volume create pathnex-volume 







