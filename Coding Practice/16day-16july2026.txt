---
# tasks file for  ravi
- name: install apache HTTP server
  yum:
    name: httpd
    state: present

- name: deploy configuration template
  template:
    src: httpd.conf.j2
    dest: /etc/httpd/conf/httpd.conf
    mode: '0644'
  notify: restart httpd

- name: Ensure httpd is running and enabled
  service:
    name: httpd
    state: started
    enabled: true


resource "aws_dynamodb_table" "ravitable" {
  name           = "raviTrainingTable"
  billing_mode   = "PAY_PER_REQUEST"
  hash_key       = "ID"

  attribute {
    name = "ID"
    type = "S"
  }


apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ravi-db
spec:
  serviceName: "ravi"
  replicas: 2
  selector:
    matchLabels:
      app: ravi-db
  template:
    metadata:
      labels:
        app: ravi-db
    spec:
      containers:
        - name: db
          image: mysql
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "admin123"


SERVICE="sshd"

if systemctl is-active --quiet $SERVICE; then
  echo "$SERVICE is running"
else
  echo "$SERVICE is NOT running"
fi


pipeline {
    agent server001
    environment {
        INSTITUTE_NAME = " ravi"
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('Deploy') {
            steps {
                input message: "Promote artifacts to staging?"
                sh 'echo "Promoting artifacts for $INSTITUTE_NAME"'
            }
        }
    }
}


tages:
  - build
  - promote

build:
  stage: build
  image: maven:3.8.1-jdk-21
  script:
    - mvn clean package
  artifacts:
    paths:
      - target/*.jar
    expire_in: 1 week

promote:
  stage: promote
  image: alpine:latest
  script:
    - echo "Promoting artifacts for  ravi"
  when: manual

